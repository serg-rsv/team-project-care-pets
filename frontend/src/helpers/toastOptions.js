import { Flip } from 'react-toastify';
const options = {
  position: 'top-left',
  autoClose: 3000,
  hideProgressBar: false,
  newestOnTop: false,
  rtl: false,
  theme: 'light',
  transition: Flip,
};

export default options;
