import { Flip } from 'react-toastify';
const options = {
  position: 'top-left',
  autoClose: 3000,
  hideProgressBar: false,
  newestOnTop: false,
  rtl: false,
  theme: 'dark',
  transition: Flip,
};

export default options;
